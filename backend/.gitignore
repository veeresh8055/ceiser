# Ignore dependency installs in any service folder.
**/node_modules/

# Ignore environment files in any service folder.
**/.env

# Firebase Admin credentials must remain local and never be committed.
services/auth/serviceAccountKey.json
